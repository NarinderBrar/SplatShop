{
  "name": "SplatShop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Babylon.js WebGPU-first Gaussian splat editor prototype.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "@babylonjs/core": "^8.20.0",
    "@playcanvas/splat-transform": "2.3.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^7.2.0"
  }
}
